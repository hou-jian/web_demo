## 用途

备份学习过程中写过的 Demo，便于复用。

## 启动

对于有要求的 demo，会在对应文件夹 README.md 写出。

其他的启动本地服务器运行即可，如：

- vscode 的 Live Server 插件。

  > 注意一个坑：文件上传 demo 中，在上传了文件后，被这个插件检查到了项目变动，导致刷新 `index.html` 。

-  `http-server` ：在项目中 `npx http-server -p 8888`

